name: Hardware Test

on:
  push:
    branches: [dev, feature/*]
  pull_request:
    branches: [dev, main]
  workflow_dispatch:

jobs:
  build:
    name: Build Firmware
    runs-on: [self-hosted, hardware]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Try to download artifact from Build workflow
        id: download-build-artifact
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v3
        with:
          workflow: build.yml
          name: firmware-pico_1motor_endless
          path: ./build-artifact/
          search_artifacts: true
          if_no_artifact_found: warn
      
      - name: Check if we need to build
        id: check-build
        run: |
          if [ -f ./build-artifact/firmware.uf2 ]; then
            echo "✓ Found firmware from Build workflow (3x faster, ubuntu-latest)"
            echo "need_build=false" >> $GITHUB_OUTPUT
            mkdir -p .pio/build/pico_1motor_endless/
            cp ./build-artifact/firmware.uf2 .pio/build/pico_1motor_endless/
          else
            echo "⚠ No Build workflow artifact found, building locally on hardware runner"
            echo "need_build=true" >> $GITHUB_OUTPUT
          fi
      
      - name: Setup Python venv
        if: steps.check-build.outputs.need_build == 'true'
        run: |
          python3 -m venv $HOME/ci-venv || true
          echo "$HOME/ci-venv/bin" >> $GITHUB_PATH
      
      - name: Install dependencies
        if: steps.check-build.outputs.need_build == 'true'
        run: |
          pip install --upgrade pip
          pip install platformio pyserial pygame
      
      - name: Build firmware locally
        if: steps.check-build.outputs.need_build == 'true'
        run: platformio run -e pico_1motor_endless
      
      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-hw-test-${{ github.run_number }}
          path: .pio/build/pico_1motor_endless/firmware.uf2
          retention-days: 7

  deploy:
    name: Deploy to Device
    needs: [build]
    runs-on: [self-hosted, hardware]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python venv
        run: |
          python3 -m venv $HOME/ci-venv || true
          echo "$HOME/ci-venv/bin" >> $GITHUB_PATH
      
      - name: Install PlatformIO
        run: |
          pip install --upgrade pip
          pip install platformio
          
      - name: Download firmware artifact
        uses: actions/download-artifact@v4
        with:
          name: firmware-hw-test-${{ github.run_number }}
          path: ./
      
      - name: Prepare firmware for upload
        run: |
          mkdir -p .pio/build/pico_1motor_endless/
          cp firmware.uf2 .pio/build/pico_1motor_endless/firmware.uf2
          ls -lh .pio/build/pico_1motor_endless/firmware.uf2

      - name: Unmount RP2040 BOOTSEL volume (if mounted)
        run: |
          entries="$(lsblk -nrpo NAME,LABEL,MOUNTPOINT 2>/dev/null | awk '$2=="RPI-RP2" && $3!="" {print $1" "$3}')"
          if [ -n "$entries" ]; then
            echo "Found mounted RP2040 BOOTSEL volume(s):"
            echo "$entries"
            while read -r dev mnt; do
              [ -n "$dev" ] || continue
              [ -n "$mnt" ] || continue
              echo "Unmounting $dev from $mnt"
              umount "$mnt" || true
            done <<< "$entries"
            udevadm settle || true
          else
            echo "No mounted RPI-RP2 volume found"
          fi

      - name: Ensure upload port is free
        run: |
          echo "Checking upload port: /dev/ttyACM0"
          if command -v fuser >/dev/null 2>&1; then
            fuser -v /dev/ttyACM0 || true
            pids="$(fuser /dev/ttyACM0 2>/dev/null || true)"
            if [ -n "$pids" ]; then
              echo "Terminating processes using /dev/ttyACM0: $pids"
              kill -TERM $pids || true
              sleep 1
              kill -KILL $pids || true
            fi
            fuser -v /dev/ttyACM0 || true
          fi
          udevadm settle || true
      
      - name: Upload firmware to device
        run: |
          platformio run -e pico_1motor_endless --target upload || {
            echo "Upload failed once; retrying after short settle..."
            sleep 2
            udevadm settle || true
            platformio run -e pico_1motor_endless --target upload
          }
      
      - name: Wait for device reset
        run: |
          for i in $(seq 1 20); do
            if [ -e /dev/ttyACM0 ]; then
              echo "Device back on /dev/ttyACM0"
              exit 0
            fi
            sleep 1
          done
          echo "ERROR: /dev/ttyACM0 not present after upload"
          ls -l /dev/ttyACM* || true
          exit 1

  test-connectivity:
    name: Test - System Connectivity
    needs: [deploy]
    runs-on: [self-hosted, hardware]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python venv
        run: |
          python3 -m venv $HOME/ci-venv || true
          echo "$HOME/ci-venv/bin" >> $GITHUB_PATH
      
      - name: Run connectivity test
        run: bash ./test/run_ci_tests.sh connectivity
        env:
          SERIAL_PORT: /dev/ttyACM0
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-connectivity-${{ github.run_number }}
          path: |
            test/ci_hardware_test.log
            test/ci_hardware_test.json
          retention-days: 30

  test-position:
    name: Test - Motor Position
    needs: [deploy]
    runs-on: [self-hosted, hardware]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python venv
        run: |
          python3 -m venv $HOME/ci-venv || true
          echo "$HOME/ci-venv/bin" >> $GITHUB_PATH
      
      - name: Run position test
        run: bash ./test/run_ci_tests.sh position
        env:
          SERIAL_PORT: /dev/ttyACM0
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-position-${{ github.run_number }}
          path: |
            test/ci_hardware_test.log
            test/ci_hardware_test.json
          retention-days: 30

  test-midi:
    name: Test - MIDI Response
    needs: [deploy]
    runs-on: [self-hosted, hardware]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python venv
        run: |
          python3 -m venv $HOME/ci-venv || true
          echo "$HOME/ci-venv/bin" >> $GITHUB_PATH
      
      - name: Run MIDI test
        run: bash ./test/run_ci_tests.sh midi
        env:
          SERIAL_PORT: /dev/ttyACM0
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-midi-${{ github.run_number }}
          path: |
            test/ci_hardware_test.log
            test/ci_hardware_test.json
          retention-days: 30

  test-sweep:
    name: Test - Motor Sweep
    needs: [deploy]
    runs-on: [self-hosted, hardware]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python venv
        run: |
          python3 -m venv $HOME/ci-venv || true
          echo "$HOME/ci-venv/bin" >> $GITHUB_PATH
      
      - name: Run sweep test
        run: bash ./test/run_ci_tests.sh sweep
        env:
          SERIAL_PORT: /dev/ttyACM0
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-sweep-${{ github.run_number }}
          path: |
            test/ci_hardware_test.log
            test/ci_hardware_test.json
          retention-days: 30

  test-scaling:
    name: Test - Joystick Scaling
    needs: [deploy]
    runs-on: [self-hosted, hardware]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python venv
        run: |
          python3 -m venv $HOME/ci-venv || true
          echo "$HOME/ci-venv/bin" >> $GITHUB_PATH
      
      - name: Run scaling test
        run: bash ./test/run_ci_tests.sh scaling
        env:
          SERIAL_PORT: /dev/ttyACM0
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-scaling-${{ github.run_number }}
          path: |
            test/ci_hardware_test.log
            test/ci_hardware_test.json
          retention-days: 30
